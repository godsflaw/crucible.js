'use strict';

import {
  addressSchema,
  bytes32Schema,
  bytesSchema,
  numberSchema,
  wholeNumberSchema,
} from './basicTypeSchemas';

export const schemas = {
  addressSchema,
  numberSchema,
  bytesSchema,
  bytes32Schema,
  wholeNumberSchema,
};
